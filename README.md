# VBST
A simple Binary Search Tree implementation that is built to handle any abstract data type that can be ordinated by numeric keys. It uses normal ints in the node keys, but this can be changed depending on the memory capacity or real numbers necessity.
