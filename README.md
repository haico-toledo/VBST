# VBST
A simple Binary Search Tree implementation that is built to handle any abstract data type that can be ordered by numeric keys. It uses normal ints in the node keys, but this can be modified depending on the developer's needs.
